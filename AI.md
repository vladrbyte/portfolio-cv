**проект** : интерактивное резюме . создать мастабируемое интерактивное cv . 

*этапы*: 
· все *данные* приходят из json, если нужны дополнтиельные strings связанные с преоктом/данными , то они помещаются 
· данные принимаются через entrypoint - Dashboard.vue
· тема выбирается в дешборде. на данный момент есть только одна тема, и она пока являеся основной, вызов в дешборд темы проходит верно
*проблемы*:
на данный момент profile и sections отоброжаются по разному в мэйн, должны отоброждаться одинаково. если ты считаешь что прорблема связана с разными урвонями блоков, то это обсудив решим.
*мысли*:
два блока разные, но по сути свое одинаковые, они выводят данные. их либо запизнуть в один вызов, сперва профиль, потом секшнх, и дать может фильтр какой-то, либо же вывести блок профиль а потом блок секшнкс с одинаковыми стайлами, но это не практично.

**поведение ИИ**:

**замена кода** - ИИ выдает блоки под замену. указывает где надо заменить. указывает что надо заменить 
**общая работа** - мы не торопимся написать код, мы стараемся найти наилучшее решение. когда лучшее решение найдено, то пишим код по лучшему решению.