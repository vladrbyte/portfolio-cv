1. *раставляем данные так json, что бы они хорошо отоброжались, и никогда не ломались, к примеру из-за того, что вдург названия ключа поменяется* 
статус: в процессе
-------------------------------------------------------------
2. *проверяем через тему, рабоатет все так как надо из пункта 1*
статус: в процессе
--------------------------------------------------------------


*Грубый план действий (пошаговый)*
----------------------------------------
**Шаг 1**. Проектирование и рефакторинг JSON-структуры (cv_2.json)

Определяем финальные ключи для config, profile и sections.

Приводим все items во всех секциях к единому, строгому стандарту.

**Шаг 2**. Создание слоя адаптера (useCv.js)

Пишем composable, который принимает cvData.

Реализуем метод защищенного чтения текста с поддержкой фолбэков (если нет текущего языка — берем defaultLang, если нет его — берем первую попавшуюся строку или "").

**Шаг 3**. Перестройка и подключение компонента темы (DossierLayout.vue)

Переписываем шаблон на использование данных из useCv.

Проверяем рендеринг секций, переключение языков, отображение списков и карточек.

**Шаг 4**. Финальная шлифовка и проверка краш-тестами

Нарочно удаляем некоторые ключи из JSON и проверяем, что UI не падает и отображается корректно.
--------------------------------------------------

*Сетка профиля (Profile):* - придумаем еще, но по хорошему это будет каверлдист с фото и со всеми персоональными данными
*Поддержка разной глубины (sections[i].depth):*- была такуя задумка, но она была выполнена не точно, это тоже обсудим.
*Верстка уникальной L-образной навигации*- сперва данные правильно расставим.

- [ ] data flows right
- [ ] theme: dossier

------------------bit later-------------------------
- [ ] "редактор" текста, в начале экарн с можержимым текста показывается прервернутым и зеркально отображенным, есть две кнопки , покрутить и развернуть и подсказа показывающая на них. потом экран остается стоять в выбранной позиции. короче начально положение этого скрина повернут на 90/180 градуов и отзеркален, дальше запоминает выбранное положение

